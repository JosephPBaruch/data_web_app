XMLHttpRequests

  Note: The below tabbed code wont work within node (will get a reference error). It will work within a browsers engine though. Add this to a <script> tag in an html document. 
    - When seeing videos online they used xmlhttprequests in a js file but they open it with a browswer. 

      //var XMLHttpRequest = require("xmlhttprequest").XMLHttpRequest;
      var xhr = new XMLHttpRequest();
      /*
      xhr.open( "GET", "https://api.apis.guru/v2/list.json", true );

      xhr.responseType = 'json';

      xhr.send();

      // the response is {"message": "Hello, world!"}
      xhr.onload = function() {
        let responseObj = xhr.response;
        //alert(responseObj.message); // Hello, world!
        console.log(responseObj);
      };
      */